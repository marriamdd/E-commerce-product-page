import styled from "styled-components";

export default function CartComponent() {
  return <CartContainer>CartComponent</CartContainer>;
}

const CartContainer = styled.div`
  position: absolute;
  z-index: 99999999999;
`;
